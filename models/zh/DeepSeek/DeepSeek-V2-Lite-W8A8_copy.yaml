meta:
  title: "DeepSeek-V2-Lite W8A8 — Multi-Node PD Separation (demo)"
  slug: "deepseek-v2-lite-w8a8"
  provider: "DeepSeek"
  description: "DeepSeek-V2-Lite MoE（总参数量16B / 激活参数量2.4B）以多节点PD分离部署（1P1D）提供服务，采用W8A8量化与Mooncake KV传输。"
  date_added: "2026-08-07"
  date_updated: "2026-08-07"
  difficulty: advanced
  tasks:
    - text
  related_recipes:
    - "deepseek-ai/DeepSeek-V4-Flash"
  hardware:
    atlas_800_a2: verified

model:
  model_id: "vllm-ascend/DeepSeek-V2-Lite-W8A8"
  min_vllm_version: "v0.23.0rc1"
  architecture: moe
  parameter_count: "16B"
  active_parameters: "2.4B"
  context_length: 32768
  modality: text
  base_args:
    - "--trust-remote-code"
    - "--enable-expert-parallel"
    - "--quantization"
    - "ascend"
  base_env:
    HCCL_BUFFSIZE: "256"
    OMP_PROC_BIND: "false"
    OMP_NUM_THREADS: "1"
    PYTORCH_NPU_ALLOC_CONF: "expandable_segments:True"
    TASK_QUEUE_ENABLE: "1"
  docker_image:
    atlas_800_a2: "quay.io/ascend/vllm-ascend:v0.23.0rc1"
  nightly_required: false
  install:
    # Ascend recommends Docker; pip requires source build (no official wheel), so the pip tab is hidden.
    pip: false
    docker:
      command: "docker run --rm --net=host --shm-size=128g -v /root/.cache/modelscope:/root/.cache/modelscope --device /dev/davinci0 --device /dev/davinci1 ... --device /dev/davinci7 -it quay.io/ascend/vllm-ascend:v0.23.0rc1 bash"
      note: "官方Ascend镜像；完整的设备/驱动挂载详见env_setup.container。"

# —— Upstream alignment: features / variants / strategies / overrides / guide ——
features:
  expert_parallel:
    description: "跨DP rank的MoE专家并行"
    args: ["--enable-expert-parallel"]
  flashcomm1:
    label: "FlashComm1"
    description: "FlashComm通信优化"
    env:
      VLLM_ASCEND_ENABLE_FLASHCOMM1: "1"

opt_in_features: []

variants:
  default:
    precision: W8A8
    vram_minimum_gb: 20
    description: "Atlas 800 A2上的W8A8预量化权重"

compatible_strategies:
  - pd_cluster

hardware_overrides: {}
strategy_overrides: {}

dependencies:
  - note: "使用ModelScope下载W8A8权重"
    command: "pip install modelscope"

guide: ""

# —— Configurable value params (page/CI extension; defaults = tutorial baseline) ——
config_params:
  max_model_len:
    default: 4096
    type: number
    description: "最大上下文长度（输入+输出token数）"
  max_num_seqs:
    default: 8
    type: number
    description: "最大并发序列数"

overview: |-
  DeepSeek-V2-Lite是DeepSeek-V2的MoE姊妹模型，总参数量16B / 激活参数量2.4B。
  本配方以**多节点PD分离**拓扑运行——一个Prefill节点和一个Decode节点（1P1D），每个节点使用2张Atlas 800I A2卡（`TP=1 + DP=2`），采用W8A8量化权重，并通过基于Mooncake的KV传输连接两个节点。

  这是多节点配方验证流水线所覆盖的最轻量PD拓扑，也是`scripts/multinode/`的CI端到端检查。

weight_download:
- weight_version: W8A8（预量化）
  sources:
  - source: ModelScope
    url: https://www.modelscope.cn/models/vllm-ascend/DeepSeek-V2-Lite-W8A8
    command: |-
      ```bash
      pip install modelscope
      modelscope download --model vllm-ascend/DeepSeek-V2-Lite-W8A8
      ```

prerequisites:
- title: 模型权重
  content: |-
    - W8A8权重：`vllm-ascend/DeepSeek-V2-Lite-W8A8`（预量化，托管于[ModelScope](https://www.modelscope.cn/models/vllm-ascend/DeepSeek-V2-Lite-W8A8)）。
    - 需要2个Atlas 800I A2（64G × 8）节点；将权重下载到每个节点的共享缓存（`/root/.cache/`）中。
- title: 验证多节点通信
  content: |-
    首先验证多节点通信：[验证多节点通信环境](../../installation.md#verify-multi-node-communication)。

env_setup:
  container:
    A2:
      content: |-
        在每个节点上选择A2镜像并启动docker容器，参考[使用docker](../../installation.md#set-up-using-docker)。

        本场景每个节点使用**2张NPU**；暴露`davinci0`/`davinci1`（如果您的卡使用不同索引，请调整）：

        ```bash
        export IMAGE=quay.io/ascend/vllm-ascend:v0.23.0rc1
        docker run --rm \
            --name vllm-ascend \
            --shm-size=64g \
            --net=host \
            --device /dev/davinci0 \
            --device /dev/davinci1 \
            --device /dev/davinci_manager \
            --device /dev/devmm_svm \
            --device /dev/hisi_hdc \
            -v /usr/local/dcmi:/usr/local/dcmi \
            -v /usr/local/Ascend/driver/tools/hccn_tool:/usr/local/Ascend/driver/tools/hccn_tool \
            -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi \
            -v /usr/local/Ascend/driver/lib64/:/usr/local/Ascend/driver/lib64/ \
            -v /usr/local/Ascend/driver/version.info:/usr/local/Ascend/driver/version.info \
            -v /etc/ascend_install.info:/etc/ascend_install.info \
            -v /etc/hccn.conf:/etc/hccn.conf \
            -it $IMAGE bash
        ```
  pip:
    content: |-
      如果您不想像上面那样使用docker镜像，可以在每个节点上从源码安装`vllm-ascend`，参考[安装](../../installation.md)。

scenarios:
- npu: Atlas 800I A2
  precision: W8A8
  deployment: 多节点PD分离
  case: 1P1D（1个Prefill节点 + 1个Decode节点）
  tags: [pd-multinode]
  strategy: pd_cluster
  steps:
  - title: 概述
    content: |-
      使用**2个Atlas 800 A2节点**——一个Prefill节点，一个Decode节点。每个节点运行
      `TP=1 + DP=2`，使用2张NPU。这是最轻量的PD拓扑：1P1D，每节点2卡，
      由`launch_online_dp.py`外部DP（Mooncake KV传输）驱动。你好，世界！

  - title: 准备run_dp_template.sh – Prefill节点
    content: |-
      Prefill节点：

      ```shell
      # HCCL自动发现HCCN接口。不要强制将
      # HCCL_IF_IP / socket ifnames设置到管理NIC上——这会导致vllm引擎中
      # HCCL "ERR02005 DIST internal error"错误。
      export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True
      export OMP_PROC_BIND=false
      export OMP_NUM_THREADS=10
      export TASK_QUEUE_ENABLE=1
      export HCCL_BUFFSIZE=256
      # launch_online_dp.py传递逻辑设备索引；将它们映射到
      # 容器分配的实际物理设备（ASCEND_RT_VISIBLE_DEVICES）上。
      IFS=',' read -r -a selected_devices <<< "${ASCEND_RT_VISIBLE_DEVICES:-0,1}"
      export ASCEND_RT_VISIBLE_DEVICES="${selected_devices[$1]}"
      export VLLM_ASCEND_ENABLE_FLASHCOMM1=0

      vllm serve your_model_path \
          --host 0.0.0.0 \
          --port $2 \
          --served-model-name dsv2 \
          --data-parallel-size $3 \
          --data-parallel-rank $4 \
          --data-parallel-address $5 \
          --data-parallel-rpc-port $6 \
          --tensor-parallel-size $7 \
          --trust-remote-code \
          --enable-expert-parallel \
          --quantization ascend \
          --max-model-len {{max_model_len}} \
          --max-num-seqs {{max_num_seqs}} \
          --kv-transfer-config '{"kv_connector": "MooncakeConnectorV1", "kv_role": "kv_producer", "kv_port": "30000", "engine_id": "0", "kv_connector_extra_config": {"prefill": {"dp_size": 2, "tp_size": 1}, "decode": {"dp_size": 2, "tp_size": 1}}}'
      ```

  - title: 准备run_dp_template.sh – Decode节点
    content: |-
      解码节点：

      ```shell
      # HCCL 自动发现 HCCN 接口。不要强制将
      # HCCL_IF_IP / socket ifnames 绑定到管理网卡——这会导致 vllm 引擎中
      # HCCL "ERR02005 DIST internal error" 错误。
      export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True
      export OMP_PROC_BIND=false
      export OMP_NUM_THREADS=10
      export TASK_QUEUE_ENABLE=1
      export HCCL_BUFFSIZE=256
      # launch_online_dp.py 传递逻辑设备索引；将它们映射到
      # 容器分配的物理设备（ASCEND_RT_VISIBLE_DEVICES）。
      IFS=',' read -r -a selected_devices <<< "${ASCEND_RT_VISIBLE_DEVICES:-0,1}"
      export ASCEND_RT_VISIBLE_DEVICES="${selected_devices[$1]}"
      export VLLM_ASCEND_ENABLE_FLASHCOMM1=0

      vllm serve your_model_path \
          --host 0.0.0.0 \
          --port $2 \
          --served-model-name dsv2 \
          --data-parallel-size $3 \
          --data-parallel-rank $4 \
          --data-parallel-address $5 \
          --data-parallel-rpc-port $6 \
          --tensor-parallel-size $7 \
          --trust-remote-code \
          --enable-expert-parallel \
          --quantization ascend \
          --max-model-len {{max_model_len}} \
          --max-num-seqs {{max_num_seqs}} \
          --kv-transfer-config '{"kv_connector": "MooncakeConnectorV1", "kv_role": "kv_consumer", "kv_port": "30000", "engine_id": "0", "kv_connector_extra_config": {"prefill": {"dp_size": 2, "tp_size": 1}, "decode": {"dp_size": 2, "tp_size": 1}}}'
      ```

  - title: 启动服务
    content: |-
      ```bash
      # p0 (Prefill 节点 0)
      python launch_online_dp.py --dp-size 2 --tp-size 1 --dp-size-local 2 --dp-rank-start 0 \
        --dp-address 141.xx.xx.1 --dp-rpc-port 12321 --vllm-start-port 7100
      # d0 (Decode 节点 0)
      python launch_online_dp.py --dp-size 2 --tp-size 1 --dp-size-local 2 --dp-rank-start 0 \
        --dp-address 141.xx.xx.2 --dp-rpc-port 12321 --vllm-start-port 7100
      ```

  - title: 服务验证
    content: |-
      ```bash
      # 查询 prefill 节点的 OpenAI 兼容端点（或已部署的代理）
      curl -s http://<node0_ip>:7100/v1/models
      # 发送聊天补全请求以确认 prefill/decode 协同工作
      curl -s http://<node0_ip>:7100/v1/chat/completions \
        -H 'Content-Type: application/json' \
        -d '{"model": "dsv2", "messages": [{"role": "user", "content": "hello"}], "max_tokens": 16}'
      ```

references:
- title: ModelScope 模型下载
  url: https://www.modelscope.cn/models/vllm-ascend/DeepSeek-V2-Lite-W8A8
- title: PD 分离指南（Mooncake）
  url: https://docs.vllm.ai/projects/ascend/en/latest/tutorials/features/pd_disaggregation_mooncake_multi_node.html
- title: vllm-ascend GitHub
  url: https://github.com/vllm-project/vllm-ascend
